# MYSQL SETUP
MYSQL_DATABASE=wordpress
MYSQL_USER=wpuser
# MYSQL_PASSWORD, MYSQL_ROOT_PASSWORD -> now provided via Docker secrets (see secrets/)

# WORDPRESS SETUP
DOMAIN_NAME=mgarnier.42.fr
WP_TITLE=Inception
WP_ADMIN=mgarnier
# WP_ADMIN_PASSWORD -> now provided via Docker secrets (see secrets/wp_admin_password.txt)
WP_ADMIN_EMAIL=mgarnier42@example.com
WP_USER=editor
# WP_USER_PASSWORD -> now provided via Docker secrets (see secrets/wp_user_password.txt)
WP_USER_EMAIL=editor@example.com