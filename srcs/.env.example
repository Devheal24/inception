# IMAGE ISO
ALPINE_IMAGE=alpine:3.23@sha256:<digest>

# MYSQL SETUP
MYSQL_DATABASE=wordpress
MYSQL_USER=wpuser
# MYSQL_PASSWORD, MYSQL_ROOT_PASSWORD -> provided via Docker secrets, see secrets/ below

# WORDPRESS SETUP
DOMAIN_NAME=mgarnier.42.fr
WP_TITLE=Inception
WP_ADMIN=mgarnier
# WP_ADMIN_PASSWORD -> provided via Docker secrets, see secrets/credentials.txt below
WP_ADMIN_EMAIL=admin@example.com

# ---
# This is a template: copy to .env and fill in real values.
# cp srcs/.env.example srcs/.env
#
# Passwords are NOT set here — they live in git-ignored files under secrets/:
#   secrets/db_password.txt        -> MariaDB wpuser password
#   secrets/db_root_password.txt   -> MariaDB root password
#   secrets/credentials.txt        -> WordPress admin password
# Create these three files locally (one password per file, no trailing content)
# before running `docker compose up`.
